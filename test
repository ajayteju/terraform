


variable private_key_location {}

connection {
    type = "ssh"
    host = "self.public_ip"
    user = "ec2-user"
    private_key = file(var.private_key_location)
}

provisioner "remote-exec" {
    inline = [
        "export ENV=dev"
        "mkdir newdir"
    ]
}